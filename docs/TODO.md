[x] Remove the cb binary from git and add it to .gitignore
[x] Fix cb start to create the initial sessions in the worktree directory that they create
[_] Fix cb start to be able to attach to the tmux session when it's not ran inside of tmux
[_] Add ESC as a hot key to close the TUI in addition to q
[_] Add a `make install` command that installs the latest version of the utility
